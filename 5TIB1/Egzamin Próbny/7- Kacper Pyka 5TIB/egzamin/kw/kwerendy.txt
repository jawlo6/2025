1. SELECT MIN(priorytet) FROM `notatki` WHERE Osoby_id=3;
2. SELECT nazwa, priorytet FROM `notatki` WHERE nazwa LIKE "%na%";
3. SELECT imie, notatki.nazwa FROM `osoby` JOIN notatki ON notatki.Osoby_id=osoby.id WHERE priorytet="5";
4. SELECT imie, COUNT(notatki.id) FROM osoby JOIN notatki ON notatki.Osoby_id=osoby.id GROUP BY Osoby_id;