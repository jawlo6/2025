SELECT imie, pensja FROM pracownicy WHERE staz < 5;

SELECT AVG(pracownicy.pensja), stanowiska.nazwa FROM pracownicy, stanowiska WHERE pracownicy.stanowiska_id=stanowiska.id GROUP BY stanowiska.nazwa;

SELECT imie, nazwisko, MAX(pensja) FROM pracownicy;

UPDATE pracownicy SET staz = staz+1;